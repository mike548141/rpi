# pathscan globs for this repo.
#
# docs/ROADMAP.md is GENERATED by atelier's board tool and rebuilt on every state
# change, so it cannot carry an inline `pathscan:allow` marker — a rebuild would
# strip it. Its two false-positive shapes are structural to the generator: the
# per-section narrative links use the link TEXT "<section>/README.md" (a real
# file, but at docs/roadmap/<section>/, not repo root), and the preamble names
# the generator as `tools/board.py` (atelier's tree, not this one).
docs/ROADMAP.md
