__pycache__/
*.pyc

# Build output (never committed; regenerated by `python -m build` / pip install -e)
build/
dist/
*.egg-info/

# Generated at release time by tools/gen_sbom.py; not committed (see ADR 0005)
sbom.cdx.json
*.cdx.json

# macOS litter
.DS_Store

# Claude Code session policy — local only, never published. The allowlist used to be
# committed; on a PUBLIC repo it maps out exactly which commands run unprompted, which
# turns prompt-injection via issues/PRs from a guess into a plan (2026-07-30 review).
.claude/settings.json
.claude/settings.local.json
